/**
* _islower - function to check for lowercase character
* @c: is the integer that will be used for the function's argument
* Return: 0
*/
int _islower(int c)
{
if (c >= 'a' && c <= 'z')
{
return (1);
}
else
return (0);
}
